Este proyecto es para almacenar un repositorio git local las diferentes configuraciones:
1. Se crea un repositorio local en alguna ubicación local
git init
git add file.yml (el nombre del archivo se debe de llamar como el spring.application.name)
git commit -m "Comentarios"
